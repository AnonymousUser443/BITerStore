# 梨苑儿（BITerStore）项目交接文档

最后更新：2026-09-15 14:04（UTC+8）

当前工作分支：`codex/liyuaner-brand`

当前已推送功能提交及生产版本：`8f1d8337d30d90277d5f3f6a395c5ceb4a567fed`

当前上游 PR：[#23](https://github.com/AnonymousUser443/BITerStore/pull/23)（Open，目标为 `main`）

生产站点：<https://store.young581.com>

> 产品对外中文品牌已经统一为“梨苑儿”；`BITerStore` 继续作为 GitHub 仓库、npm 包、环境变量、数据库/存储键和其他兼容性技术标识使用。本文以当前代码和生产环境为准；接手开发和运维时，应优先参考本文、`AGENTS.md`、实际代码及生产配置。

## 0. 当前接手快照

### 0.1 代码、分支与生产

- 本地仓库：`F:\BITerstore\app`
- 当前分支：`codex/liyuaner-brand`
- 当前 HEAD：`8f1d8337d30d90277d5f3f6a395c5ceb4a567fed`
- Young581 远端：`fork`；`fork/codex/liyuaner-brand` 与当前 HEAD 完全一致。
- 上游远端：`origin` = `AnonymousUser443/BITerStore`；`origin/main` 当前为 `eae519221b70c672775ed83fa3182492f1c3952e`。
- 上游状态：PR [#21](https://github.com/AnonymousUser443/BITerStore/pull/21) 和 [#22](https://github.com/AnonymousUser443/BITerStore/pull/22) 已合并；品牌 PR [#23](https://github.com/AnonymousUser443/BITerStore/pull/23) 仍为 Open，目标为 `main`，交接时包含 1 个提交、34 个变更文件。
- `origin/main` 已包含品牌提交的父提交 `4f16531`，因此 PR #23 相对 `main` 只有品牌提交，不夹带上一批安全整改。
- 生产 SSH 使用本机别名 `young-remote`；HostName、用户、端口和凭据只保留在安全渠道/本机 SSH 配置中，不写入仓库。
- 生产目录：`/home/young/biterstore`
- 生产版本记录：`/home/young/biterstore/.release-commit`
- 当前生产版本：`8f1d8337d30d90277d5f3f6a395c5ceb4a567fed`
- 上一生产版本：`b76e82512a2891fa42acfc8972899a8a43a9ede0`
- 当前生产前端入口：`js/app.4ab0d4ef.js`
- 生产目录是发布快照，不是 Git 仓库，不能直接执行 `git pull`。

交接时本地工作区相对已推送提交仅保留以下内容：

- `docs/项目交接文档.md`：本次更新后的交接信息，尚未提交；
- `docs/品牌更名记录-2026-09-15.md`：本地已更新实际交付状态，并删除了不应写入 Git 的具体 AppID 文本，尚未提交；
- `Assets/generated/biterstore-app-icon.png`：既存未跟踪用户素材，未修改、未删除，禁止擅自提交。

上述两份文档修改都不在当前 PR 和生产快照中。后续若要提交或推送，必须先检查差异并重新取得负责人明确确认；不要沿用本轮已经消费完毕的远程写入授权，也不要为整理历史而擅自 amend/force-push。

2026-09-15 最后一次生产验收结果：8 个 Compose 容器全部为 running，重启次数均为 0；`bit-login`、PostgreSQL、Redis 健康；Prisma 共 7 个 migration，数据库为最新状态；生产 API `/api/v1/health` 返回 `status=ok`。公网首页、商品列表、管理后台和 BIT-Login 均返回 200，HSTS/CSP 存在，H5、管理后台和 Swagger 已按 UTF-8 原始内容确认显示“梨苑儿”。`deploy/.env`、`deploy/secrets/` 和持久化卷在发布中保留，未复制或输出任何密钥。

### 0.2 最近完成的用户可见修复

最近几轮已经完成：

- 将 Web/H5、微信小程序、管理后台、服务端、分享文案、Swagger/TOTP 标识和相关文档的对外中文品牌统一为“梨苑儿”，同时保留兼容性技术标识；
- 完成第二批安全与运营整改：实时身份/session 校验、Cookie 会话、TOTP 和举报限流、举报目标/去重、图片魔数/结构/尺寸校验、pending 清理、公共安全响应头、`MUTED` 服务端边界、注销商品收敛及媒体访问边界；
- 依赖审计强制在线优先，修复 `adm-zip`/`js-yaml` 路径并在生产 H5 构建中通过审计；
- Web 与 MiniProgram 的 owner 商品详情、状态更新、举报入口、账号缓存隔离、登录失效回退和媒体上传凭据处理已经对齐；
- 微信构建、私有 AppID/基础库注入、包体门禁和开发者工具自动化闭环可用；
- 较早完成商品收藏/联系、BigInt 会话、删除/已售二次确认、响应式布局、发布图片布局、个人数据快照、消息排序/封面、返回状态保持、HTTPS/Cookie 会话和 isbn.work 精确匹配等修复。

关键提交按时间倒序：

| 提交 | 内容 |
| --- | --- |
| `8f1d833` | 统一启用“梨苑儿”中文品牌；已部署，PR #23 Open |
| `eae5192` | 上游合并 PR #22 |
| `4f16531` | 记录第二批生产部署验证 |
| `b7694eb` | 上游合并 PR #21 |
| `676ef0c` | 完成第二批安全与运营整改 |
| `97fe3f7` | 加固客户端依赖构建和在线审计 |
| `1d830648` | 标记已售前要求显式二次确认 |
| `43d63c14` | 接入 isbn.work 并严格匹配 ISBN |
| `9b775f7` | 检测 Cloudflare 下的非安全访问 |
| `888e98e` | 强制浏览器 HTTPS 会话 |
| `71d9e4c` | 稳定浏览器登录会话 |
| `8381045` | 微信登录改为合法域名下的同域代理 |
| `93f942b` | 稳定微信构建、基础库同步和 E2E |
| `33acc24a` | 移除商品详情划线原价 |
| `0df95059` | 优化商品详情信息展示 |
| `ac85b56f` | 返回页面时保持界面稳定 |
| `969624e2` | 稳定消息排序并展示真实封面 |
| `57ce71c5` | 移除消息卡片延迟闪烁 |
| `d613cdae` | 完整显示会话并打开关联商品 |
| `e371893c` | 消息即时显示并关联商品 |
| `4bae4c8e` | 个人数据使用本地快照即时显示 |
| `8b6f0f8f` | 将删除确认改为站内弹窗 |
| `06677892` | 合并 PR8 流式布局并保留 PR7 修复 |
| `ce032091` | 修复含 BigInt 消息的会话加载 |
| `c64b2910` | 修复商品收藏和联系操作 |

### 0.3 最近验证状态

- Web：lint、测试 `40/40`、production build 全部通过；
- Admin：测试 `8/8`、production build 全部通过；
- Server：lint、测试 `91/91`、production build 全部通过；
- MiniProgram：lint、测试 `48/48`、H5 build、WeApp build 全部通过；
- 正式 WeApp 包检查：94 个文件、0 个 WebP、17 个页面脚本；私有配置中的真实 AppID 与基础库版本已正确注入最终 `dist`，具体值不得写入 Git；
- `weapp:doctor`：开发者工具登录、服务端口、私有 AppID 和构建产物检查全部通过；
- 微信自动化：长会话中“我的发布”和“收藏/个人资料/重置”各出现过一次 automator 通信超时，随后分别以独立干净会话复跑通过，应用 console error 与 exception 均为 0；不要把它描述成单次完整流程毫无超时；
- H5 视觉巡检覆盖 65 个页面/视口组合，`diagnostics: []`；
- 在线依赖审计通过：10 条依赖路径涉及 8 个已审查 advisory，当前复核期限为 2026-12-03；
- 生产构建与验收通过：新 H5 入口 `js/app.4ab0d4ef.js`，内外网健康、公开列表、后台、登录入口、品牌和安全响应头均已复核。

## 1. 一分钟了解项目

梨苑儿（技术仓库名 `BITerStore`）是面向北京理工大学校内场景的二手书平台。目前已具备：

- H5 与微信小程序双端客户端；
- BIT-Login 校园身份认证，微信作为已认证账号的可选绑定与快捷登录；
- 商品发布、搜索、收藏、上下架、聊天、通知、举报和管理后台；
- 强制上传封面和 ISBN 页，ISBN 条码识别及免费书目元数据回填；
- Cloudflare R2 私有对象存储，图片统一通过受控媒体接口读取；
- PostgreSQL、Redis、Nginx、BIT-Login、API、H5、管理后台和备份容器；
- Cloudflare Worker 书目代理和外部隧道入口。

当前生产环境运行正常。版本 `45365dc` 首次完成以下性能和上架策略调整：

- 打开 H5 时不再等待会话校验后才渲染整页，会话在后台验证；
- 首页、分类、详情和“我的发布”优先显示本地快照，再后台刷新；
- 发布、改状态和删除后立即更新本地快照；
- 已通过校园认证的用户发布后直接进入 `ACTIVE`，不再进入待审核；
- 部署迁移已把原有已认证用户的待审核商品改为在售。

交接时数据库非删除商品状态汇总为：`ACTIVE=2`、`SOLD=1`，已认证用户的 `PENDING_REVIEW=0`。这是交接时快照，不是长期业务指标。

## 2. 必须遵守的工程规则

开始修改前先阅读仓库根目录的 `AGENTS.md`。最重要的约束是：

1. `web/` 是视觉和行为 Golden Reference，不得为了迁移便利而删除、重命名或重设计。
2. 可见页面修改后，至少运行 `miniProgram` 的 lint、test、H5 build 和 WeApp build。
3. 微信开发者工具服务端口可用时，还要运行仓库内微信 E2E，并检查 route、稳定 E2E 元素、console/exception、结构快照和截图。
4. 平台差异必须封装在 `miniProgram/src/platform/`，页面不能直接散落浏览器或微信专属 API。
5. 禁止提交 `.env`、私钥、CLI token、开发者工具私有配置和真实失败工件。
6. 禁止自动上传微信体验版、提审或发布，也不得修改用户 AppID。
7. 推送、生产部署和其他远程写入操作，每次都必须先得到负责人明确确认。

仓库可能存在未跟踪的 `Assets/generated/`，它属于现有素材工作区。除非任务明确要求，不要删除、覆盖或提交。

## 3. 系统架构

```text
浏览器 / 微信小程序
        │
        ├── H5 静态资源 ────────────────┐
        ├── /api/v1/* ───────────────┐  │
        └── /bit-login/* ──────────┐ │  │
                                   ▼ ▼  ▼
Cloudflare 隧道 → Nginx → BIT-Login / NestJS API / Taro H5 / Admin
                              │          │
                    PostgreSQL + Redis   ├── 私有 R2 图片
                              │          └── Cloudflare 书目 Worker
                              └── 本机定时 pg_dump
```

| 目录/服务 | 技术 | 职责 |
| --- | --- | --- |
| `miniProgram/` | Taro 4.2.1、React 18、TypeScript | 正式双端客户端；构建 H5 和微信小程序 |
| `web/` | React 19、vinext | H5 的 Golden Reference、共享页面组件、CSS 和 H5 Repository |
| `server/` | NestJS 11、Fastify、Prisma | 认证、用户、商品、图片、聊天、治理、书目接口 |
| `admin/` | React 19、Vite | 管理和治理后台 |
| `deploy/` | Docker Compose、Nginx | 生产编排、网关、BIT-Login overlay、备份 |
| `deploy/book-metadata-worker/` | Cloudflare Worker | 受限 ISBN 书目代理，不是通用代理 |
| PostgreSQL | PostgreSQL 17 | 业务主数据 |
| Redis | Redis 7 | 登录轮询状态、书目缓存等临时状态 |
| R2 | Cloudflare R2 | 生产图片对象存储，Bucket 保持私有 |

### H5 与微信端的关系

- H5 生产镜像由 `miniProgram/Dockerfile.h5` 构建。
- 构建时会复制 `web/`，H5 路由直接复用 `web/app/components/mobile-app.tsx` 和 `web/app/globals.css`。
- 微信端使用 `miniProgram/src/pages/` 中的平台页面，并通过 `miniProgram/src/platform/` 处理平台差异。
- 未配置真实 API 时可以运行 DemoRepository；真实登录后由 Repository 切换到 API 实现。

## 4. 核心业务约束

### 4.1 身份与权限

- 游客可以浏览、搜索和查看商品。
- 收藏、发布、私聊及个人数据需要登录。
- 发布和修改商品要求 `campusStatus=VERIFIED`，由后端 `VerifiedGuard` 强制执行。
- 微信不能创建一个独立的未认证正式账号；应先用校园身份登录，再绑定微信。
- 管理接口要求 `MODERATOR`、`ADMIN` 或 `SUPER_ADMIN`，并且管理员 TOTP 已验证。
- 生产环境必须保持 `ALLOW_DEV_AUTH=false`。

### 4.2 商品状态

正常发布路径：

```text
DRAFT ──发布──> ACTIVE ──预订──> RESERVED ──成交──> SOLD
                  │                    │
                  └────下架──> OFF_SHELF <──┘
                                 │
                                 └──重新上架──> ACTIVE
```

- 已认证用户正式发布时直接写入 `ACTIVE`。
- `PENDING_REVIEW` 仍保留在数据库枚举和兼容状态机中，但不再是普通用户发布路径。
- 平台治理仍可使用 `BLOCKED` 阻止违规商品展示。
- 删除采用软删除：写入 `deletedAt` 并把状态改为 `OFF_SHELF`。
- 状态更新使用 `version` 做乐观锁，冲突时提示刷新重试。
- `clientRequestId` 对同一卖家唯一，用于防止重复提交产生重复商品。

### 4.3 图片与 ISBN

- 正式发布必须同时有一张 `COVER` 和一张 `ISBN` 图片。
- 最多关联 6 张图片：封面、ISBN 页和最多 4 张补充实拍图。
- 支持 JPEG、PNG、WebP，单张不超过 5 MB。
- ISBN 页只用于识别和校验，公共商品接口不会返回该图片。
- 生产使用私有 R2；对象先写入 `pending/`，完成校验后移动到 `media/`。
- 对外读取统一走 `/api/v1/media/:id`，不要公开 R2 Bucket。
- 识别优先使用设备条码能力，服务端另有 ZXing WASM 兜底；书目信息由受限 Worker 查询 Google Books、Open Library 和 Crossref。

### 4.4 前端缓存

- H5 使用带筛选条件的商品列表快照和按用户区分的“我的发布”快照。
- 页面先渲染快照，再请求服务器刷新，避免每次进入页面先空白 2–3 秒。
- 发布、状态变更和删除会同步更新快照。
- 登录状态会先根据本地信息渲染，再在后台调用 `/me` 校验。
- 排查“数据没更新”时先区分：界面显示的是旧快照，还是 API 本身返回旧数据。不要简单删除缓存机制来掩盖问题。

## 5. 本地开发

统一使用 Node.js 22.13.x。首次进入各子项目运行 `npm ci`，不要随意重写锁文件。

### 5.1 H5 / 微信小程序

```bash
cd miniProgram
npm ci
npm run dev:h5
# 或
npm run dev:weapp
```

环境变量模板位于 `miniProgram/.env.example`。本地真实值应放在被忽略的本地环境文件中，不得提交。

质量门禁：

```bash
npm run lint
npm test
npm run build:h5
npm run build:weapp
```

H5 视觉巡检脚本：

```bash
npm run qa:h5:visual
```

微信自动化准备和故障排查见根目录 `WEAPP_CODEX_AUTOMATION_SETUP.md`。微信开发者工具能连接时，继续运行：

```bash
npm run weapp:doctor
npm run qa:weapp
```

#### 5.1.1 当前微信 E2E 状态与配置

本机使用仓库外的便携 Node.js，普通 PowerShell 中 `npm` 可能不在 `PATH`。运行前先执行：

```powershell
cd F:\BITerstore\app\miniProgram
$NodeDir = 'F:\BITerstore\.tools\node-v22.13.1-win-x64'
$env:Path = "$NodeDir;$env:Path"

node --version
npm.cmd --version
npm.cmd run weapp:doctor
npm.cmd run qa:weapp
```

E2E 实际读取 `miniProgram/.env.weapp.local`，不是 `.env`。该文件必须保持忽略提交，并至少配置：

```dotenv
WECHAT_DEVTOOLS_CLI=<微信开发者工具 cli.bat 的本机绝对路径>
WEAPP_PROJECT_PATH=F:\BITerstore\app\miniProgram\dist
WECHAT_DEVTOOLS_SERVICE_PORT=<开发者工具“设置 → 安全设置”中显示的 HTTP 服务端口>
WEAPP_AUTOMATION_PORT=9420
WEAPP_LAUNCH_WAIT_MS=12000
WECHAT_DEVTOOLS_CLI_TOKEN=<仅在开发者工具要求 token 时本地填写>
```

注意：HTTP 服务端口和 automator WebSocket 端口 `9420` 是两个不同端口，不能混用。真实 AppID 只放在被忽略的 `miniProgram/project.private.config.json`；公开的 `miniProgram/project.config.json` 必须继续使用 `touristappid`。

2026-09-15 本机 doctor 在沙箱外运行时全部通过：CLI 路径、Node 22.13.1、开发者工具登录、服务端口、真实 AppID 注入和 WeApp 构建均正常。`scripts/sync-weapp-project-config.mjs` 已同时把私有 `AppID` 和 `libVersion` 写入 `dist/project.config.json`，并有静态回归测试；不得因此提交真实 AppID、端口、CLI token 或开发者工具私有配置。最终 `miniProgram/dist` 已在 E2E 后重新执行正式 `build:weapp`，不是 H5 或测试构建。

本轮微信自动化覆盖登录合法域名、引导/游客首页、搜索/详情/收藏/联系、发布、已售确认、通知/聊天、我的发布、收藏/个人页/重置和全状态页，并检查 route、稳定 `e2e-*` 元素、console/exception、结构快照和截图。一次完整长会话在“我的发布”和“收藏/个人资料/重置”阶段出现 automator 通信超时；两个场景随后分别在独立干净会话中复跑通过，应用 console error `0`、exception `0`。证据在本地忽略目录 `qa-artifacts/weapp/`；不得提交真实失败工件。真机网络、微信授权弹窗、硬件相册/扫码和返回栈仍需至少一台实际设备抽查。

已知的本地构建缓存注意事项：Taro Webpack/SWC 增量缓存曾将少数冷页生成为只有页面标题、`root.cn=[]` 的空模块。如果页面只显示标题/底部栏，且对应页面 JS 异常小，先关闭开发者工具，只删除可重建的 `miniProgram/.swc` 和 `miniProgram/node_modules/.cache/webpack`，再重新执行 E2E WeApp 构建。不要删除源码、私有配置或失败现场以外的用户文件。

### 5.2 后端

```bash
cd server
npm ci
npx prisma generate
npm run dev
```

后端环境变量模板在 `server/.env.example`。本地需要 PostgreSQL 和 Redis。检查命令：

```bash
npm run lint
npm test
npm run build
```

开发数据库迁移使用 `npm run prisma:migrate`；生产只允许 `npm run prisma:deploy`，不要在生产运行 `prisma migrate dev`。

### 5.3 Golden Reference

```bash
cd web
npm ci
npm run dev
```

检查命令：

```bash
npm run lint
npm test
npm run build
```

### 5.4 管理后台和书目 Worker

```bash
cd admin
npm ci
npm test
npm run build

cd ../deploy/book-metadata-worker
npm ci
npm test
```

Worker 部署需要 Cloudflare 授权，必须单独获得负责人确认。

## 6. 配置和密钥交接

真实密钥不得写进本文或 Git。接手人应通过密码管理器或线下安全渠道取得以下内容：

| 配置 | 生产位置/管理位置 | 说明 |
| --- | --- | --- |
| Compose 环境变量 | 服务器项目目录的 `deploy/.env` | 数据库、JWT、R2、微信、书目代理等 |
| BIT-Login 注册私钥 | `deploy/secrets/bit-login-registration-private.pem` | 仅服务器可读，不得离机明文传播 |
| BIT-Login 公钥 | `BIT_LOGIN_PUBLIC_KEY_PEM` | 由 API 验证注册 JWT |
| R2 S3 凭据 | Cloudflare R2 API Tokens + `deploy/.env` | 只授予目标 Bucket Object Read & Write |
| Worker 共享密钥 | Cloudflare Worker Secret + `deploy/.env` | 两端的 `BOOK_METADATA_PROXY_TOKEN` 必须一致 |
| Google Books Key | Cloudflare Worker Secret | 可选；不存在时回退到其他免费源 |
| 微信 AppID/Secret | 微信开放平台/公众平台 + `deploy/.env` | 小程序和网站应用分开配置 |
| 管理员 TOTP 加密密钥 | `deploy/.env` | 不得变更，否则现有密文可能无法使用 |
| SSH 和隧道凭据 | 线下交接 | 主机、端口、SSH key、Cloudflare 隧道 token 不入库 |

环境模板见：

- `deploy/.env.example`
- `server/.env.example`
- `miniProgram/.env.example`
- `web/.env.example`

更换任何认证或存储密钥前，先确认轮换影响和回滚方案。尤其不要直接覆盖 JWT、TOTP 或 BIT-Login 密钥。

## 7. 当前生产环境

- 生产站点：<https://store.young581.com>
- SSH：使用本机别名 `young-remote`；具体 HostName、用户、端口和凭据不写入本文或 Git。
- 服务器项目目录：`/home/young/biterstore`
- Compose 文件：`deploy/config.yaml`
- 版本标记：项目根目录 `.release-commit`
- 当前版本：`8f1d8337d30d90277d5f3f6a395c5ceb4a567fed`
- 上一版本：`b76e82512a2891fa42acfc8972899a8a43a9ede0`
- 当前 H5 入口：`js/app.4ab0d4ef.js`
- 网关监听服务器回环地址，由外部隧道转发；PostgreSQL 和 Redis 不暴露公网端口。
- 当前服务：`postgres`、`redis`、`bit-login`、`api`、`h5`、`admin`、`nginx`、`backup`。
- API 健康检查：<https://store.young581.com/api/v1/health>

生产目录当前是发布快照，不是 Git 工作区。不要在服务器上执行 `git pull` 并假设能更新代码。

### 7.1 最近一次生产发布（2026-09-15）

- 发布对象为 Git archive 生成的提交快照 `8f1d8337d30d90277d5f3f6a395c5ceb4a567fed`，SHA-256 为 `c8710dbf17bc7aa3f2b41922d2943c81e30c3b310bc564f88cc9fd55ac00d0a0`。
- 上传前检查 471 个归档条目，不含真实 `.env`、私有 AppID 配置、`deploy/secrets/`、`node_modules/`、`dist/`、QA 工件或 `Assets/generated/`。
- 服务器端复核文件大小与 SHA-256 一致后才解压；`deploy/.env`、`deploy/secrets/` 和持久化卷均保留。
- `docker compose -f deploy/config.yaml up -d --build` 成功构建并切换 API、H5、Admin 和 BIT-Login，随后重启 Nginx；生产 H5 在线依赖审计通过。
- 8 个容器均为 running，重启次数为 0；Nginx `nginx -t` 通过；API 近 10 分钟错误模式计数为 0；Prisma 检查确认 7 个 migration 全部最新。
- 内网和公网验收均通过：首页、`/api/v1/health`、`/api/v1/listings?limit=50`、`/admin/` 和 `/bit-login/` 正常；H5、后台和 Swagger 均确认“梨苑儿”生效；HSTS 与 CSP 存在。
- 验收完成后才写入 `.release-commit`；远端和本地临时发布包、远端探针文件均已删除，均可从 Git 提交重新生成。
- 该功能提交已经部署，但上游 PR #23 仍未合并。PR 合并产生的新 merge SHA 不代表功能变化；不要仅因 merge SHA 不同就无验证地改写生产版本标记。

## 8. 发布流程

以下为当前发布方式。命令中的 SSH 主机和端口必须从安全渠道取得，不要写入脚本或提交到仓库。

### 8.1 发布前

1. 确认目标提交已经存在，记录短 SHA。
2. 检查 `git status --short`，不要把其他人的未提交改动带入发布包。
3. 根据变更范围运行测试；可见页面变更必须完成双端门禁。
4. 检查 Prisma migration 是否只做可预期、可回滚或可恢复的数据变化。
5. 再次取得负责人对“部署该具体提交”的明确确认。

### 8.2 制作并上传发布包

建议使用 `git archive`，这样不会带入 `.env`、本地依赖、构建产物或未跟踪文件：

```powershell
$Release = "<commit-sha>"
git archive --format=tar.gz --output="biterstore-$Release.tar.gz" $Release
Get-FileHash "biterstore-$Release.tar.gz" -Algorithm SHA256
scp -P <ssh-port> "biterstore-$Release.tar.gz" <ssh-user>@<ssh-host>:/home/<ssh-user>/
```

先在服务器复核 SHA-256，再解压。`deploy/.env`、`deploy/secrets/`、`deploy/backups/` 和 Docker volumes 不在 Git archive 中，正常情况下会保留。

### 8.3 构建和切换

在服务器项目目录执行：

```bash
tar -xzf /home/<ssh-user>/biterstore-<commit-sha>.tar.gz
docker compose -f deploy/config.yaml up -d --build
docker compose -f deploy/config.yaml restart nginx
```

API 容器启动命令会自动运行 `npx prisma migrate deploy`，迁移成功后才启动 NestJS。

`nginx` 重启不能省略：Compose 重建 `h5` 或 `api` 后容器 IP 可能改变，而当前 Nginx 会缓存启动时解析到的上游地址。未重启时首页或接口可能短暂返回 502。

确认所有验收通过后，再更新 `.release-commit` 并删除发布临时包。不要在构建失败时写入成功版本号。

### 8.4 发布后验收

```bash
docker compose -f deploy/config.yaml ps
docker compose -f deploy/config.yaml logs --tail=100 api
curl -fsS http://127.0.0.1:<bind-port>/api/v1/health
curl -fsS -o /dev/null -w '%{http_code}\n' http://127.0.0.1:<bind-port>/
```

还必须从公网检查：

- `/` 返回 200，页面能正常渲染；
- `/api/v1/health` 返回 `status=ok`；
- `/api/v1/listings?limit=50` 返回 200；
- HTML 引用的 `js/app.<hash>.js` 是本次构建的新资源；
- 登录、查看列表、进入“我的发布”和发布流程无控制台异常；
- 涉及数据库迁移时，检查 `_prisma_migrations` 和对应业务数据结果。

不要只看 `docker compose up` 的退出码就宣布发布成功。

## 9. 回滚原则

当前部署目录不是原子化 release/symlink 结构，回滚能力有限。发布前应保留上一版本的 Git SHA 和发布包。

代码回滚的一般步骤：

1. 明确导致问题的版本和上一稳定 SHA；
2. 检查新 migration 是否向后兼容；
3. 使用上一稳定 SHA 重新制作并上传 Git archive；
4. 解压并重新执行 `docker compose -f deploy/config.yaml up -d --build`；
5. 重启 `nginx`；
6. 完成与正常发布相同的内外网验收。

Prisma migration 默认不会自动回滚。若新版本已经写入不可逆数据，不得只回退代码；应先制定单独的数据恢复方案。不要手工删除 `_prisma_migrations` 记录。

中长期建议把服务器改造成版本化 release 目录加原子 symlink，并建立自动保留上一版镜像的流程。

## 10. 日常运维

### 10.1 常用只读检查

```bash
cd /home/young/biterstore
cat .release-commit
docker compose -f deploy/config.yaml ps
docker compose -f deploy/config.yaml logs --tail=100 api
docker compose -f deploy/config.yaml logs --tail=100 nginx
docker stats --no-stream
```

数据库只读统计应通过 `postgres` 容器内的 `psql` 执行。不要把数据库密码写到命令历史或聊天记录。

### 10.2 常见故障

| 现象 | 优先检查 | 处理方向 |
| --- | --- | --- |
| 首页 502，但 API 正常 | `nginx` 日志是否连接旧 H5 IP | 重启 `nginx`，再检查 H5 容器 |
| API 502/504 | API 容器、迁移日志、PostgreSQL/Redis 健康 | 先读日志，不要反复重建数据库容器 |
| 页面进入时像整页重载 | 是否仍加载旧 JS、Service Worker、登录接口延迟 | 核对资源 hash、浏览器控制台和 `/me` 时延 |
| 列表先空白几秒 | 快照是否写入、筛选 key 是否一致、API 是否失败 | 检查 Repository 快照和后台刷新，不要移除缓存兜底 |
| 发布后仍待审核 | API 是否为旧版本、迁移是否执行 | 检查 `.release-commit`、API 镜像和商品状态 |
| 图片上传失败 | R2 CORS、预签名过期、Content-Type/Length | 核对 Bucket CORS 和上传三步流程 |
| ISBN 有号码但书名不合适 | Worker 数据源顺序、缓存和中文标题选择 | 检查 Worker 响应与 Redis 缓存，必要时清理单个 ISBN 缓存 |
| SSH 偶发断开 | 本地代理链路、连接超时、服务端网络 | 使用较短的幂等命令重试，先确认上一条命令是否已执行 |

## 11. 数据与备份

- PostgreSQL 数据位于 Docker volume `postgres-data`。
- Redis 使用 `redis-data`，它不是业务主数据的唯一来源。
- BIT-Login challenge/session SQLite 位于 `bit-login-data`。
- 本机图片模式使用 `api-uploads`；生产当前使用 R2，但该 volume 仍保留。
- `backup` 容器每天生成一次 PostgreSQL custom-format dump，保存到 `deploy/backups/`，保留 7 天。

重要限制：

- 当前 Compose 中的数据库备份文件没有实际加密，即使环境模板存在 `BACKUP_ENCRYPTION_PASSPHRASE`，编排也尚未使用它。
- 备份仍在同一台服务器，不是异地备份。
- R2 对象、BIT-Login SQLite、密钥和 `.env` 不包含在 PostgreSQL dump 中。
- 尚未在本文交接前完成一次正式的全量恢复演练。

建议最高优先级完成：加密异地备份、R2/SQLite/配置备份清单，以及季度恢复演练。

## 12. 已知问题和后续优先级

### 当前交付事项

1. **PR #23 待审/待合并**：品牌提交已经推送并部署，但上游 PR 仍为 Open。仓库当前没有 `.github/workflows/`，不能把“没有 GitHub Checks”误认为 CI 已通过；验收依据是本文记录的本地和生产门禁。
2. **两份文档修改尚未提交**：本交接文档和 `docs/品牌更名记录-2026-09-15.md` 的最新状态只在本地。下一对话应先检查 diff，再询问是否以普通后续提交更新 PR #23；不得擅自 amend 或 force-push。
3. **品牌记录脱敏**：已推送的 `8f1d833` 曾在品牌记录中写出具体 AppID。本地已删除具体值，仅保留“由私有配置注入”的验证结论。AppID 不是 AppSecret，但仍违反本仓库“不提交真实 AppID”的约束；后续应在获得推送确认后把本地修订加入 PR，且不得输出或提交 AppSecret、CLI token 等真正凭据。
4. **生产先于合并**：生产已运行 PR #23 的 head 提交。若 PR 审查要求代码变更，必须重新跑与变更范围相称的门禁并重新取得部署确认；不能假设现网已经自动跟随 PR。

### P0：上线前/尽快处理

1. **备份体系**：实现加密异地备份并做真实恢复演练。
2. **Nginx 动态上游**：解决容器重建后缓存旧 IP 的问题，避免依赖人工重启。
3. **R2 生命周期**：当前 S3 凭据缺少 Bucket Settings 权限，`pending/` 1 天清理规则仍需用 Cloudflare 账户 API Token 配置并验证。
4. **微信真机验收**：完成至少一台真机的登录、图片、扫码、发布、聊天和返回栈冒烟。
5. **合规确认**：确认微信主体、服务类目、备案域名、隐私指引及 BIT-Login 自托管许可和维护责任。

### P1：稳定性与可维护性

1. 将手工发布改为版本化、可回滚的自动化流程，但仍保留人工确认门禁。
2. 建立健康监控、错误告警、接口延迟和关键业务指标面板。
3. 为发布、上传、认证、聊天和管理操作补充生产级集成测试。
4. 评估 Taro 官方兼容版本升级，处理锁定依赖树中的上游安全告警；不要使用 `npm audit fix --force`。
5. 当前生产 H5 入口 JS 约 288 KiB、入口 CSS 约 85.7 KiB；继续拆分冷页面并建立可持续的性能预算。

### P2：产品与数据质量

1. 统计 ISBN 识别和中文书目命中率，再决定是否接入更多中文数据源或本地 OCR。
2. 完善管理后台审核、举报处置和审计检索体验。
3. 增加封闭灰度用户访谈、运营指标和数据保留策略。

平台当前不提供在线支付、物流、评价、复杂推荐或音视频聊天。新增这些能力前必须重新评估产品、合规和安全边界。

## 13. 新接手人的第一天建议

1. 获取仓库访问权限和安全渠道中的生产凭据，但不要复制密钥到个人笔记或聊天。
2. 阅读 `AGENTS.md`、本文、`deploy/README.md` 和 `WEAPP_CODEX_AUTOMATION_SETUP.md`。
3. 使用 Node.js 22.13.x 安装依赖，跑通后端测试及 `miniProgram` 双端质量门禁。
4. 只读检查生产 `.release-commit`、Compose 状态、API 健康和最近日志。
5. 用测试账号走一遍校园登录、查看列表、上传封面/ISBN 页、识别、发布和“我的发布”。
6. 确认 Cloudflare、微信平台、服务器、域名和代码仓库各自的负责人及紧急联系人。
7. 在做第一个生产变更前，先完成一次无写入的发布演练和回滚桌面演练。

## 14. 交接确认清单

- [ ] 代码仓库和分支权限已移交
- [ ] 生产 SSH 信息已通过安全渠道移交
- [ ] Cloudflare 账户、R2、Worker 和隧道权限已移交
- [ ] 微信小程序/开放平台权限已移交
- [ ] BIT-Login 许可、密钥保管人和维护责任已确认
- [ ] 管理员账号及 TOTP 恢复流程已移交
- [ ] 生产 `.env` 各变量用途已逐项说明
- [ ] 数据库、R2、BIT-Login 和配置备份责任人已确认
- [ ] 发布、验收、回滚和故障升级流程已演示
- [ ] 当前 P0 风险已创建负责人和计划日期

交接完成后，由接收人和原负责人共同更新本文的日期、生产版本和未完成清单。

## 15. 可直接交给下一对话的开场提示

```text
请继续接手梨苑儿（技术仓库名 BITerStore）项目。先做只读核对，不要重复已经完成的更名、测试或部署。

本地仓库：F:\BITerstore\app
当前分支：codex/liyuaner-brand
当前 HEAD：8f1d8337d30d90277d5f3f6a395c5ceb4a567fed
Young581 同名分支：8f1d8337d30d90277d5f3f6a395c5ceb4a567fed
上游 main：eae519221b70c672775ed83fa3182492f1c3952e（已合并 PR #21、#22）
当前 PR：https://github.com/AnonymousUser443/BITerStore/pull/23
PR 状态：Open，base=main，head=Young581:codex/liyuaner-brand；交接时 1 个提交、34 个变更文件
当前生产版本：8f1d8337d30d90277d5f3f6a395c5ceb4a567fed
上一生产版本：b76e82512a2891fa42acfc8972899a8a43a9ede0
生产站点：https://store.young581.com
当前 H5 入口：js/app.4ab0d4ef.js
服务器项目目录：/home/young/biterstore
SSH 使用本机别名 young-remote；具体 HostName、用户、端口和凭据不要写入文档、命令脚本或 Git。

开始前请完整阅读：
- AGENTS.md
- docs/项目交接文档.md
- docs/品牌更名记录-2026-09-15.md
- docs/项目排查报告-2026-09-13.md
- deploy/README.md
- TODO.md
- WEAPP_CODEX_AUTOMATION_SETUP.md

当前工作区不是 clean，这是有意保留的状态：
- M docs/项目交接文档.md：最新交接文档，尚未提交；
- M docs/品牌更名记录-2026-09-15.md：已更新 PR/部署状态并删除具体 AppID，尚未提交；
- ?? Assets/generated/biterstore-app-icon.png：用户既存素材，未修改，禁止擅自删除、覆盖或提交。

先运行 git status、git diff --check，并分别审阅两份文档 diff。不要 stash、reset、checkout 或清理这些文件。若用户希望把文档加入 PR，先取得本轮新的提交/推送确认，再用普通后续提交推送；不要 amend 或 force-push。当前对话中的推送和部署授权已经用完，不能沿用。

web/ 是视觉和行为 Golden Reference；正式双端客户端是 miniProgram/，生产 H5 由 miniProgram/Dockerfile.h5 构建并复用 web/。产品对外中文品牌是“梨苑儿”；BITerStore、@biterstore/*、BITERSTORE_*、数据库/Bucket/容器/备份名、Cookie/LocalStorage/IndexedDB key、audience=biterstore、历史昵称兼容和书目 User-Agent 等技术标识被有意保留，不要继续全局替换。

不要提交 .env、密钥、SSH 信息、真实 AppID、AppSecret、微信 CLI token、服务端口、开发者工具私有配置、qa-artifacts 或真实失败工件。公开 miniProgram/project.config.json 必须继续使用 touristappid；真实 AppID 只来自被忽略的 project.private.config.json，并可注入被忽略的 dist。

所有远程写入、提交后的推送、生产部署、微信上传、提审或发布，都必须重新取得我的明确确认。生产目录是发布快照，不是 Git 仓库。

已经完成且不要重复：
- 8f1d833 将 Web/H5、微信小程序、管理后台、服务端和分享/文档的对外品牌统一为“梨苑儿”；
- 分支已推送，PR #23 已创建，且 PR 相对 origin/main 只有这 1 个品牌提交；
- 同一提交已经通过 Git archive 发布至 young-remote；.release-commit 与 HEAD 一致；
- 生产 8 个 Compose 容器均 running、重启次数均为 0，API health=ok，数据库 7 个 migration 最新；
- 公网首页、公开商品列表、管理后台和 BIT-Login 均为 200，HSTS/CSP 存在；H5、后台和 Swagger 已确认显示“梨苑儿”；
- 发布临时包和探针文件已清理，可从 Git 提交重新生成。

最后一次代码验证：
- Web lint、40/40 tests、production build 通过；
- Admin 8/8 tests、production build 通过；
- Server lint、91/91 tests、production build 通过；
- MiniProgram lint、48/48 tests、H5/WeApp build 通过；
- WeApp 包为 94 个文件、0 个 WebP、17 个页面脚本，weapp:doctor 全部通过；
- H5 65 个页面/视口视觉巡检 diagnostics=[]；
- 在线依赖审计通过，当前复核期限为 2026-12-03。

微信自动化的准确边界：完整长会话中“我的发布”和“收藏/个人资料/重置”各出现一次 automator 通信超时，随后两个场景分别在独立干净会话中复跑通过，console error=0、exception=0。不要宣称单次长会话完全无超时；真机登录、授权弹窗、相册/扫码、真实上传和返回栈仍需设备抽查。E2E 后已重新构建正式 WeApp，当前 miniProgram/dist 不是 H5/测试包。

当前首要接手动作：
1. 只读运行 git status、git fetch --prune origin/fork，并核对 PR #23 与远端 head；网络或远程写入按规则处理。
2. 只读检查 young-remote 的 .release-commit、docker compose ps、API health 和最近错误日志；不要因为 PR 尚未合并而重新部署。
3. 审阅本地两份文档的 diff，向用户确认是否提交并推送到 PR #23；没有确认就保持本地。
4. 后续优先事项是 PR #23 审查/合并、微信真机冒烟、加密异地备份与恢复演练、R2 pending 生命周期规则、Nginx 动态上游和监控告警。
5. 如果 PR 审查带来代码变化，重新执行与变更范围相称的门禁，并在任何生产更新前再次取得明确部署确认。
```
