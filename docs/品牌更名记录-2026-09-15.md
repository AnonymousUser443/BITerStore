# 梨苑儿品牌更名记录

日期：2026-09-15

项目：Biterstore / `BITerStore`

新中文品牌：**梨苑儿**

## 变更结论

梨苑儿已经作为产品对用户展示的统一中文品牌，覆盖 Web/H5、微信小程序、管理后台、服务端文案、分享文本和项目文档。原有 `BITerStore` 仅在仓库、包名、环境变量、存储键、接口兼容字段、部署资源名、GitHub 地址和上游 User-Agent 等技术边界中保留，避免破坏已有数据、缓存、部署和集成。

## 已覆盖的界面与文案

- Web/H5：启动页、欢迎页、登录页、商品封面、状态页、SEO/Open Graph 元数据和 `og.png` 品牌图。
- 微信小程序：启动页、欢迎页、登录隐私说明、首页标题、商品封面、状态页、系统公告、分享文本、导航栏标题、项目显示名和正式构建产物。
- 管理后台：页面标题、登录门禁、侧栏品牌和治理文案。
- 服务端：Swagger 标题、种子公告、管理员 TOTP issuer；中文 issuer 在 `otpauth` URL 中经过 URL 编码，保留标签分隔符语义。
- 文档与测试：README、实施计划、QA 记录、开发指南，以及对应品牌断言和 TOTP URL 测试。

## 有意保留的技术标识

以下内容没有替换：

- GitHub 仓库路径、npm 包名和 `BITERSTORE_*` 环境变量；
- 数据库、Bucket、容器、备份、Cookie、LocalStorage、IndexedDB 和内部事件键；
- `audience=biterstore`、迁移脚本中的历史昵称兼容规则，以及书目代理 User-Agent；
- `BITer` 默认昵称和历史用户昵称，它们属于数据兼容或校园身份，不是品牌展示文案。

## 验证记录

- Web lint、测试（40/40）和生产构建：通过。
- 管理后台测试（8/8）和构建：通过。
- 服务端 lint、测试（91/91）和构建：通过。
- 小程序 lint、测试（48/48）、H5 构建：通过。
- 正式 WeApp 构建与包校验：94 个文件、0 个 WebP、17 个页面脚本；真实 AppID 为 `wx8edbb563de66dacf`，项目显示名为“梨苑儿 Taro”。
- `weapp:doctor`：开发者工具登录、服务端口、真实 AppID 和构建产物检查全部通过。
- 微信自动化：完整流程中绝大多数场景通过；两个长交互场景曾出现一次自动化通信超时。随后对“我的发布”和“收藏/个人资料/重置”分别进行独立干净会话复跑，均通过，且无异常、无可操作 console error。
- `git diff --check`：通过；旧品牌扫描结果仅剩上述有意保留的技术标识、历史资料和仓库链接。

## 交付边界

本次只完成本地代码、文档和构建验证。没有自动推送远程仓库、创建/更新 PR 或部署服务器；这些动作需在确认目标分支、PR 和服务器版本后单独执行。
