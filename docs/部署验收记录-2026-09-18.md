# 2026-09-18 部署验收记录

验收完成时间：2026-09-18 00:45（UTC+8）。用户已明确授权“推送、部署”。

## 发布结果

| 项目 | 结果 |
| --- | --- |
| 生产站点 | <https://store.young581.com> |
| 发布代码 | `b5590bb79a03f6a7ec10e8630cde55f43f06f2b1` |
| 推送分支 | `fork/codex/public-routes-chat-fixes` |
| PR | [#24](https://github.com/AnonymousUser443/BITerStore/pull/24)，已收到修复提交，仍为 Open |
| 上一生产版本 | `79560aebce401653a5e2151153d3bfca77f1790e` |
| 本次更新服务 | API、H5、Admin、backup；随后重启 Nginx |
| 生产版本标记 | 内外网验收后才将 `.release-commit` 更新为上述完整 SHA |
| H5 资源 | `js/app.b2e0996a.js`、`css/app.2afdcf42.css` |
| Admin 资源 | `assets/index-CysZHBSz.js`、`assets/index-m0-XFhBx.css` |
| 数据库迁移 | 13 个全部 current，包含 `202609180001_private_default_nicknames` |

发布包来自 `git archive HEAD`，本地和服务器 SHA-256 均为 `70e8f85c686f54acde5105d874db13b48168296bb047ec6e4d9f37929d9e3e8c`。包内没有环境文件、密钥、私有配置、依赖目录、QA 工件和 `Assets/generated/`。生产 `.env` 与 secret 文件在解包前后校验一致，数据卷保留。

这份验收记录及同步的交接文档会另作纯文档提交；其 Git HEAD 可以晚于生产代码 SHA，不代表服务代码尚未发布。

## 发布前验证与恢复保障

- 本地既有门禁：283 项测试、各端相应 lint/typecheck/build、H5 71 个目标及最终 5 个相关页面、WeApp E2E 12/12，通过情况见[修复记录](问题修复记录-2026-09-18.md)。
- 服务器使用独立镜像标签构建四个更新组件，再切换已验证镜像；H5 受控依赖审计通过。
- 临时 PostgreSQL 17／Redis 容器执行全部 13 个迁移，`npm start` 正常运行，数据库／Redis／本地存储 readiness 通过；该隔离环境不配置 BIT-Login，因此该项为 skipped。
- 执行仓库 `deploy/backup/verify-ci.sh`：真实 PostgreSQL 中验证旧默认昵称被迁移、自定义昵称保留；真实 `pg_dump` → GPG 加密 → 实际恢复脚本 → 临时数据库恢复成功。这里使用合成数据，不代表生产备份已恢复到独立灾备环境。
- Nginx 配置语法通过。
- 切换前创建生产加密备份 `deploy/backups/biterstore-predeploy-20260918-b5590bb79a03.dump.gpg`，84713 字节；解密结果与源归档一致，`pg_restore --list` 成功，临时明文已清理。
- 保留上一生产 API/H5/Admin/backup 镜像标签 `biterstore-rollback-<component>:79560aebce40`。迁移不自动回滚；旧默认昵称替换属于数据变化，不能用回退镜像恢复原值。

## 生产验收

| 检查 | 结果 |
| --- | --- |
| 服务 | 8 个 Compose 服务 running；API、PostgreSQL、Redis、BIT-Login、backup 健康检查通过 |
| Readiness | 公网连续 3 次 200，收尾内网再次 200；database、redis、storage、bitLogin 均为 ok |
| 新迁移 | Prisma status current；只读聚合确认“BITer + 本人学号”的默认昵称剩余 0 条 |
| HTTP 冒烟 | 24 项通过：首页、JS/CSS、SW、Admin、列表、真实详情、公开媒体、旧媒体路径、路由跳转、权限、BIT-Login 隔离与 readiness |
| 缓存及安全头 | 首页 CSP/HSTS 存在，列表 ETag 条件请求返回 304；本次媒体公网响应为更严格的 `no-store` |
| 私有接口 | 未登录读取单会话、owner/会话媒体及旧 `/media/` 兼容路径返回 401；媒体错误响应为 `no-store` |
| BIT-Login 边界 | 非认证 schedule 和 OpenAPI 路径均返回 404；未创建真实校园登录 challenge |
| 路由 | 未知路径相对 302 到站内 404；旧详情相对 302 到规范 query 路由，无 HTTPS 降级 |
| 浏览器 | 390×900、DPR=2，真实生产首页、真实商品详情及后台登录卡片加载；没有 mock API，已查看截图；应用 console error、运行时 exception、HTTP 5xx、缺图、横向溢出均为 0 |
| 日志 | 切换稳定后采样窗口内 API ERROR/FATAL/未处理异常及 Nginx error/crit/alert/emerg 均为 0 |

已清理本次服务器发布归档、独立验证目录及临时脚本，隔离容器和网络已销毁；生产加密备份和回滚镜像保留。浏览器截图和实际探针结果仅保存在被忽略的本机 QA 目录。

## 仍需处理或验证

1. **浏览器 CSP 提示**：生产 CSP 拦截 H5 模板的两段内联脚本及 Cloudflare 统计脚本；首页还请求了缺失的 `favicon.ico`。本次三页可用且无应用异常，但不能描述为“浏览器控制台完全零错误”。后续应将必要内联逻辑移至允许的静态脚本或采用精确 hash/nonce，并补齐 favicon、明确统计脚本策略。游客 `/me`、refresh 和后台安全状态的 401 属于未登录边界。
2. **CI 首次执行**：工作流已推送，但本次 GitHub API 查询仍没有 Actions run，不能记为 GitHub CI 通过。本次是在本地及服务器隔离环境实际完成对应门禁；PR 审阅合并、Actions 启用/执行和必需检查仍待处理。
3. **真实登录联调**：私有媒体成功响应、真实 R2 并发上传／取消、审核并发及已读并发压力仍需测试账号验证。未登录 401 检查不能代替这些场景。
4. **微信发布**：普通生产包已构建，仍需下载合法域名核对、真机私有图片与交易流程验收；未执行平台上传、提审或发布。
5. **灾备与运维**：合成数据库恢复和生产备份解密校验已完成；生产数据隔离恢复、异地备份、R2 生命周期、Cloudflare WAF/告警、容量验证和 Nginx 动态上游等事项仍按交接文档推进。
