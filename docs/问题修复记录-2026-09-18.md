# 项目问题修复记录

修复日期：2026-09-18（UTC+8）

对应审查：[项目现状复查与问题清单](项目现状复查与问题清单-2026-09-17.md)。

本次完成 C01–C09 的代码修复，并补充回归测试、CI 配置及文档校正。用户随后授权推送和部署：**修复已作为 `b5590bb` 推送至 PR #24，并于 2026-09-18 00:45（UTC+8）完成生产验收，13 个迁移 current。** 详见[部署验收记录](部署验收记录-2026-09-18.md)。`Assets/generated/` 未修改。

## 修复结果

| 编号 | 实际修改 | 验证方式 |
| --- | --- | --- |
| C01 商品复审 | 内容编辑清空的审核标记成为重新上架的必要条件；未获当前内容批准时回到待审核。管理员操作携带页面读取的版本，并在事务中条件更新；双端按服务端实际状态显示“已提交审核”。 | 覆盖编辑→下架→再上架、正常再次上架、旧审核页面及审核中并发编辑。 |
| C02 上传一致性 | R2 写入已校验字节，移除对可再次 PUT 的 pending 对象复制。每次完成请求使用独立最终键和数据库条件更新，重复请求幂等；失败请求不会删除成功请求的文件。限制 GET 读取大小，取消操作校验对象键。 | 覆盖读取后替换源对象、并发完成、数据库响应丢失和取消抢先完成；使用真实控制器与模拟 S3/数据库，另验证真实本地文件。 |
| C03 恢复脚本 | GPG 解密增加 `--yes`，允许覆盖 `mktemp` 已建立的文件。 | 本地实际 GPG 验证通过；发布前进一步在隔离 PostgreSQL 完成合成数据的真实加密归档与完整恢复。 |
| C04 默认昵称 | 新注册、恢复及历史默认昵称改为随机 `BITer-…`；新增数据迁移，仅替换与本人学号精确匹配的旧默认昵称。 | 单测及真实隔离 PostgreSQL 验证通过；生产迁移后旧默认昵称剩余 0 条。 |
| C05 旧会话 | 新增受成员权限约束的 `GET /conversations/:id`；H5 和小程序直接取单会话，不再依赖最近 50 条摘要。 | 覆盖直接读取旧会话、非成员拒绝、完整历史合并及已读。 |
| C06 非在售图片 | 卖家列表和详情使用 owner 媒体接口；会话使用成员媒体接口读取允许状态下的已审核图片。ISBN 保持私有，删除／封禁／待审核商品不向会话开放图片。微信通过带请求头的下载获取图片，退出或切换账号清理临时文件。 | 覆盖接口鉴权、查询约束、私有缓存策略、令牌刷新、不向外域发送令牌；双端构建及页面回归。真实私有图片上传和下载仍需真机联调。 |
| C07 已读游标 | 在同一条数据库更新中要求旧游标为空或小于目标，防止旧请求覆盖新进度。 | 模拟低序号请求最后完成，游标仍保持高序号。真实 PostgreSQL 并发压力验证待补。 |
| C08 禁言会话 | `MUTED` 可以登录及刷新，继续保留只读边界；`BANNED`／`DELETED` 仍拒绝发放会话。 | 覆盖禁言登录和刷新、封禁／注销拒绝；既有写操作禁言测试继续通过。 |
| C09 启动入口 | `npm start` 改为实际构建路径 `dist/src/main.js`。 | 构建通过；发布前在隔离 PostgreSQL／Redis 环境执行标准启动，readiness 通过。 |

此外修正了 Nginx `/media/` 兼容路径强制公共缓存的问题，保留上游的私有媒体缓存头；修复小程序独立类型检查发现的联合类型、异步请求测试类型及 H5 图片适配器映射问题。

审核标记复用现有 `moderationDecision`：内容编辑与清空批准发生在同一版本更新内；“忽略”操作不会覆盖已有的 `ACTIVE` 批准。历史商品若没有明确的批准标记，重新上架会进入复审，不自动推断其已经通过审核。

## 本地验证

运行时：Node.js 22.13.1；沿用现有锁文件和已安装依赖，没有批量升级依赖。

| 范围 | 结果 |
| --- | --- |
| Server | 27 个测试文件，163 项测试通过；TypeScript 检查和构建通过 |
| Web Golden Reference | 48 项测试、ESLint、生产构建通过 |
| MiniProgram | 52 项测试、ESLint、独立 TypeScript 检查通过；H5/WeApp 构建通过 |
| Admin | 9 项测试、TypeScript 和生产构建通过 |
| 书目 Worker | 11 项测试通过 |
| 测试总数 | 283 项通过（审查基线为 261 项） |
| H5 页面 | 71 个页面／视口全量巡检通过；最终类型修正后再跑 5 个相关页面，诊断均为 0；已查看关键截图 |
| 微信 DevTools | 12/12 场景通过，应用 console error 0、exception 0；检查了路由、稳定元素、截图和验收记录 |
| 最终普通 WeApp 包 | 94 文件、1296.2 KiB 原始体积、1728.3 KiB 估算上传载荷，17 个页面脚本、0 WebP，包体门禁通过 |
| 恢复脚本 | 真实 GPG 加解密通过；shell 语法检查通过，数据库部分未在本机运行 |
| CI 配置 | YAML 解析和任务结构检查通过；尚无 GitHub Actions 运行结果 |

自动化工件存放在被忽略的 `qa-artifacts/` 目录，没有加入源码。微信验收使用测试构建；完成后重新生成普通生产包。真实 AppID、开发者工具状态、私有配置及凭据未写入本文。

## 持续检查

新增 [CI 工作流](../.github/workflows/ci.yml)：

- PR、main 更新或手动触发，GitHub 权限仅 `contents: read`，不部署，不使用生产密钥。
- 使用锁文件安装 Server、Web、Admin 和 MiniProgram，执行对应 lint、typecheck、测试及构建。
- 执行 H5 性能预算和 WeApp 包体门禁，以及书目 Worker 测试。
- 使用临时 PostgreSQL 17／Redis 服务，执行 Prisma 迁移、标准启动及 readiness。
- 用 [合成数据验证脚本](../deploy/backup/verify-ci.sh) 验证历史昵称迁移和加密归档恢复至临时库，并检查 Nginx 配置语法。

工作流使用官方 [checkout v6](https://github.com/actions/checkout/tree/v6) 和 [setup-node v6](https://github.com/actions/setup-node/tree/v6)。本机没有可用 Docker／PostgreSQL；发布前已在服务器隔离容器实际完成迁移、标准启动、合成数据恢复和 Nginx 检查。**GitHub API 在推送后仍未返回 Actions run，不能记为 GitHub CI 通过。** 首次 Actions 执行及分支保护所需检查仍待处理。

## 上线后仍需完成

1. 完成真实 R2 并发上传和取消验证；已有隔离数据库迁移、标准启动、合成数据完整恢复及 Nginx 检查通过记录。
2. Server、Admin、H5 与 backup 已同步发布，昵称迁移已应用；微信平台发布仍待完成。新版后台审核请求需要 `version`，旧后台会收到冲突提示；新版聊天依赖单会话接口。
3. 微信私有图片使用 `downloadFile`，需核对 `store.young581.com` 的下载合法域名配置，并在真机验收待审核、下架、已售、退出和切换账号的图片显示。DevTools 使用测试数据，不能代替这一步。
4. Nginx 已重启并探测，私有媒体及兼容路径未登录响应均为 401／`no-store`；实际登录后的媒体成功响应仍需联调。动态上游解析和本次发现的 CSP／favicon 提示仍待处理。
5. PR 审阅合并、Cloudflare WAF／告警、异地备份、R2 生命周期、容量压测、微信平台资料和许可等外部事项继续保持待办，详见原审查报告第 5 节。

本次生产发布和已执行的外部验证见部署验收记录；其余场景不计作已验证。合成数据恢复不代替生产数据灾备，DevTools 不代替微信真机验收。
