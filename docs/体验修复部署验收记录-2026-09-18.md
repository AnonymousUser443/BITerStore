# 四项体验修复部署验收记录

完成时间：2026-09-18 01:43（UTC+8）。用户明确要求“将本次修复推送并部署”。

## 发布结果

- 站点：https://store.young581.com；后台：https://store.young581.com/admin/。
- 分支：`codex/public-routes-chat-fixes`，已推送至 fork，对应 [PR #24](https://github.com/AnonymousUser443/BITerStore/pull/24)，未合并。
- 修复提交：`a66d41fc7b772598289f50942430db6e207ad078`。
- 生产快照及 `.release-commit`：`0e12c5f22dda6dcfe52c809338bfffeda051d187`，包含修复及发布前状态文档；随后提交的验收文档不改变运行代码，无需重复部署。
- 上一生产版本：`b5590bb79a03f6a7ec10e8630cde55f43f06f2b1`。
- 更新 API、H5、Admin，并重启 Nginx；保留生产私有配置、密钥、数据库及存储卷。
- H5 资源：`js/app.be513661.js`、`css/app.70aa105d.css`。

本次上线后台待审核详情、H5 返回稳定性、滚动条隐藏和举报进度信息补全。实现及本地验证见 [四项体验问题修复记录](四项体验问题修复记录-2026-09-18.md)。微信普通生产包已经本地构建和验收，本次未上传、提审或发布微信平台版本。

## 构建与备份

1. 从精确提交生成归档，检查不包含私有配置、凭据、依赖、QA 工件或用户未跟踪素材；本地和服务器 SHA-256 一致。
2. 在独立目录构建三个镜像；H5 受控依赖审计通过，8 个已审查上游 advisory 的例外仍按原记录管理。
3. 隔离 PostgreSQL／Redis 完整应用 13 个迁移，标准 API 启动和 readiness 通过；隔离测试的 BIT-Login 为 skipped，生产另行真实检查。
4. 切换前生成 `deploy/backups/biterstore-predeploy-20260918-0e12c5f22dda.dump.gpg`（85,011 字节）；解密后逐字节比较及 `pg_restore --list` 均通过。此次未向生产执行恢复操作。
5. 旧镜像保留为 `biterstore-rollback-api:b5590bb79a03`、`biterstore-rollback-h5:b5590bb79a03`、`biterstore-rollback-admin:b5590bb79a03`。私有配置切换前后校验一致。
6. 无新增迁移；生产 13 个迁移全部 current。验收后清理临时归档、隔离目录及其容器／网络，保留加密备份和回滚镜像。

## 线上验证

- 8 个 Compose 服务均 running；API、PostgreSQL、Redis、BIT-Login 和 backup 健康检查通过。
- 公网 26 项 HTTP 检查通过：首页及版本化资源、Service Worker、后台、真实公开列表／详情／图片、ETag 304、安全响应头、未知路由与旧详情路由相对跳转、私有媒体鉴权、BIT-Login 非认证路径限制。
- 新增 `/api/v1/admin/listings/:id` 和 `/api/v1/reports/mine` 匿名请求均返回 401，响应 `no-store`。
- 公网连续 3 次 readiness 及最终内网复检通过：database、redis、storage、bitLogin 全部 ok。
- 浏览器打开真实首页、商品详情和后台登录页，检查截图；应用 exception、console error、5xx、损坏图片及横向溢出计数均为 0。
- 对真实商品连续 10 次进入详情、点击左上角返回：同一应用 DOM 保留，只有一个应用容器，滚动位置保持，滚动条样式为隐藏。
- H5／网关 Nginx 配置检查通过；运行中的 API／H5／Admin 镜像 ID 与本次构建一致；从切换开始统计 API 错误日志及 Nginx error／critical 日志均为 0。
- 完成公网验收后写入生产版本标记。

## 验证边界与剩余事项

后台已登录审核和个人历史举报完整展示由本地受控浏览器、服务端单测及微信 E2E 验证；本次未使用真实生产账号执行治理或举报写操作。生产浏览器仍有既有 CSP 拦截内联／统计脚本及 favicon 404 提示，游客身份探测返回预期的 401；不能把应用错误为 0 描述成浏览器所有诊断为 0。

GitHub API 确认 PR head 已更新，但发布提交的 Actions run 数量为 0，不能声称 CI 已通过。微信平台发布、真机联调、外部运维和依赖风险继续按交接清单推进。

实际截图、HTTP 检查和返回回归结果保存在忽略的本地 QA 目录，不提交生产工件、私密信息或真实 AppID。
